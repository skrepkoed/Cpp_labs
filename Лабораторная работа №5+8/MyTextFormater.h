#pragma once
#include <string>
#include <iostream>
#include <fstream>
#include <regex>
using namespace std;
class MyTextFormater
{
public:
	static void formatText(const string& filename);

	//static bool removePageNumber(string& line);
};

